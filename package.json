{
  "name": "mind-voice",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "curl -sSfL https://github.com/getzola/zola/releases/latest/download/zola-x86_64-unknown-linux-gnu.tar.gz | tar -xz -C /tmp && /tmp/zola build"
  }
}
